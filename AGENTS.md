# 알고리즘 풀이 문서 작성 지침

## 적용 범위

- 이 지침은 저장소 전체에 적용한다.
- 알고리즘 풀이 문서를 새로 만들거나 완성할 때는 `template.md`를 기본 구조로 사용한다.
- 기존 문서의 언어와 서식 규칙을 유지한다. 사용자가 별도로 요청하지 않는 한 설명은 한국어로 작성한다.

## 분석 기준

- 대상 Markdown 파일에 있는 기존 Java 코드를 분석할 원본 풀이로 간주한다.
- 사용자가 코드 변경을 명시적으로 요청하지 않는 한 원본 Java 코드를 다시 작성하거나 최적화하는 등 수정하지 않는다.
- `Intuition`, `Approach`, `Complexity`에는 연산 순서와 사용한 자료구조를 포함하여 실제 Java 구현의 동작을 설명한다.
- 기존 문제 정보는 유지하며, 비어 있거나 미완성인 값 중 확실하게 판단할 수 있는 항목만 채운다.

## 문서 구조

- `template.md`의 항목 순서와 Markdown 서식을 따른다.
- 템플릿의 주석과 자리표시자는 간결하고 구체적인 내용으로 교체한다.
- `Intuition`에는 관련 제약 조건, 핵심 성질이 성립하는 이유, 선택한 알고리즘이 적합한 이유를 설명한다.
- `Approach`에는 다른 풀이를 제안하지 말고 실제 구현된 알고리즘을 단계별로 설명한다.
- `Complexity`에는 입력 크기를 나타내는 변수를 정의하고 시간 복잡도와 보조 공간 복잡도의 근거를 작성한다.
- `<br>`, 구분선, 제목, 코드 블록은 템플릿 및 인접 문서와 일관되게 유지한다.

## Better Code 작성 기준

- 다음 중 하나 이상에 해당할 때만 `### Better Code (Java)` 항목을 추가한다.
  - 시간 복잡도가 실질적으로 개선되는 경우
  - 공간 복잡도가 실질적으로 개선되는 경우
  - 의미 있게 다른 알고리즘 풀이를 제시하는 경우
- 변수명 변경, 문법 축약, 조기 반환 위치 변경 등 알고리즘과 복잡도가 같은 단순 리팩터링은 Better Code로 추가하지 않는다.
- 기존 Better Code가 위 조건을 충족하지 않으면 설명과 불필요한 여백을 포함해 해당 항목 전체를 삭제한다.
- Better Code를 추가한 경우 변경점과 원본 풀이 대비 복잡도를 간단히 설명한다.

## Python 코드

- 사용자가 명시적으로 변경을 요청하지 않는 한 기존 Python 풀이를 유지한다.
- Python 코드 블록 바로 아래에는 짧은 개조식 설명만 추가한다.
- 핵심 알고리즘, 중요한 Python 특성, 시간 및 공간 복잡도를 몇 개의 간결한 항목으로 요약한다.
- Java의 `Intuition`이나 `Approach` 내용을 Python 설명에 그대로 반복하지 않는다.

## 검증

- 완성된 문서를 다시 읽고 모든 설명이 코드와 정확히 일치하는지 확인한다.
- `O()` 같은 자리표시자, 빈 코드 블록, 템플릿 주석이 의도치 않게 남아 있지 않은지 확인한다.
- 복잡도 계산에 정렬 같은 라이브러리 연산과 입력 컬렉션 복사 비용이 반영되었는지 확인한다.
